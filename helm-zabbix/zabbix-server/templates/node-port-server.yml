apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.server.name }}
spec:
  type: NodePort
  ports:
    - port: 10051
      targetPort: 10051
      nodePort: 30051
  selector:
    components: {{ .Values.server.name }}