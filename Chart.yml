apiVersion: v1
kind: Service
metadata:
  name: ksm-kube-state-metrics
spec:
  selector:
    type: NodePort
    app: ksm-kube-state-metrics
  ports:
  - port: 8080
    targetPort: 8080
    NodePort: 30808
